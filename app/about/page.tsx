import Button from '../../components/button/button'
// `app/about/page.tsx` is the UI for the `/about` URL
export default function Page() {
    return (
        <div>
            <h1 data-testid="heading">Hello, About Page!</h1>
        </div>

    );
}